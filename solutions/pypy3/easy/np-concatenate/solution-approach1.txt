# ──────────────────────────────────────────────────
# Link        https://www.hackerrank.com/challenges/np-concatenate/problem?isFullScreen=true
# Problem     Concatenate
# Difficulty  Easy
# Subdomain   Numpy
# Platform    HackerRank
# Language    pypy3
# Status      Accepted
# Submitted   2026-09-07, 03:52 p.m.
# ──────────────────────────────────────────────────

# Enter your code here. Read input from STDIN. Print output to STDOUT

import numpy as np

n, m, p = map(int, input().split())

array_1 = np.array([list(map(int, input().split())) for _ in range(n)])

array_2 = np.array([list(map(int, input().split())) for _ in range(m)])

# concatenate the two arrays along axis 0 (rows)
result = np.concatenate((array_1, array_2), axis=0)

print(result)
